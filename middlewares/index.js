const error = require('./error');
const wrapper = require('./wrapper');
const wrapperOfMiddlewares = require('./wrapperOfMiddlewares');

module.exports = {
  error,
  wrapper,
  wrapperOfMiddlewares,
};
